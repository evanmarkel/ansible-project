apiVersion: 1

providers:
  - name: "sre-host-dashboards"
    orgId: 1
    folder: "sre"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
